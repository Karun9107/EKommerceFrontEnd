import { EkommerceValidator } from './ekommerce-validator';

describe('EkommerceValidator', () => {
  it('should create an instance', () => {
    expect(new EkommerceValidator()).toBeTruthy();
  });
});
